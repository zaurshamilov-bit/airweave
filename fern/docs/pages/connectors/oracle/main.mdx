---
title: "Oracle"
description: "Oracle integration with Airweave"
---

{/* AUTO-GENERATED CONTENT START */}



<div className="connector-header">
  <img src="icon.svg" alt="Oracle logo" width="72" height="72" className="connector-icon" />
  <div className="connector-info">
    <h1>Oracle</h1>
    <p>Connect your Oracle data to Airweave</p>
  </div>
</div>

## Overview

The Oracle connector allows you to sync data from Oracle into Airweave, making it available for search and retrieval by your agents.

## Configuration


### OracleSource

Oracle source implementation.

This source connects to an Oracle database and generates entities for each table
in the specified schemas. It uses database introspection to:
1. Discover tables and their structures
2. Create appropriate entity classes dynamically
3. Generate entities for each table's data


<Card
  title="View Source Code"
  icon="brands github"
  href="https://github.com/airweave-ai/airweave/tree/main/backend/airweave/platform/sources/oracle.py"
>
  Explore the Oracle connector implementation
</Card>

### Authentication

This connector uses a custom authentication configuration class: `OracleAuthConfig`.

<Card
  title="Authentication Configuration"
  className="auth-config-card"
  style={{ backgroundColor: 'rgba(0, 0, 0, 0.1)', padding: '16px', marginBottom: '24px' }}
>

Oracle authentication configuration.
<ParamField
  path="host"
  type="str"
  required={true}
>
  The host of the Oracle database
</ParamField>
<ParamField
  path="port"
  type="int"
  required={true}
>
  The port of the Oracle database
</ParamField>
<ParamField
  path="database"
  type="str"
  required={true}
>
  The name of the Oracle database
</ParamField>
<ParamField
  path="user"
  type="str"
  required={true}
>
  The username for the Oracle database
</ParamField>
<ParamField
  path="password"
  type="str"
  required={true}
>
  The password for the Oracle database
</ParamField>
<ParamField
  path="schema"
  type="str"
  required={false}
>
  The schema of the Oracle database
</ParamField>
<ParamField
  path="tables"
  type="str"
  required={false}
>
  Comma separated list of tables to sync. For example, 'users,orders'. For all tables, use '*'
</ParamField>
</Card>





{/* AUTO-GENERATED CONTENT END */}
