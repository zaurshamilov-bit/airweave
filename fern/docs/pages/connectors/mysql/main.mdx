---
title: "Mysql"
description: "Mysql integration with Airweave"
---

{/* AUTO-GENERATED CONTENT START */}



<div className="connector-header">
  <img src="icon.svg" alt="Mysql logo" width="72" height="72" className="connector-icon" />
  <div className="connector-info">
    <h1>Mysql</h1>
    <p>Connect your Mysql data to Airweave</p>
  </div>
</div>

## Overview

The Mysql connector allows you to sync data from Mysql into Airweave, making it available for search and retrieval by your agents.

## Configuration


### MySQLSource

MySQL source implementation.

This source connects to a MySQL database and generates entities for each table
in the specified schemas. It uses database introspection to:
1. Discover tables and their structures
2. Create appropriate entity classes dynamically
3. Generate entities for each table's data


<Card
  title="View Source Code"
  icon="brands github"
  href="https://github.com/airweave-ai/airweave/tree/main/backend/airweave/platform/sources/mysql.py"
>
  Explore the Mysql connector implementation
</Card>

### Authentication

This connector uses a custom authentication configuration class: `MySQLAuthConfig`.

<Card
  title="Authentication Configuration"
  className="auth-config-card"
  style={{ backgroundColor: 'rgba(0, 0, 0, 0.1)', padding: '16px', marginBottom: '24px' }}
>

MySQL authentication configuration.
<ParamField
  path="host"
  type="str"
  required={true}
>
  The host of the MySQL database
</ParamField>
<ParamField
  path="port"
  type="int"
  required={true}
>
  The port of the MySQL database
</ParamField>
<ParamField
  path="database"
  type="str"
  required={true}
>
  The name of the MySQL database
</ParamField>
<ParamField
  path="user"
  type="str"
  required={true}
>
  The username for the MySQL database
</ParamField>
<ParamField
  path="password"
  type="str"
  required={true}
>
  The password for the MySQL database
</ParamField>
<ParamField
  path="schema"
  type="str"
  required={false}
  default="public"
>
  The schema of the MySQL database
</ParamField>
<ParamField
  path="tables"
  type="str"
  required={false}
  default="*"
>
  Comma separated list of tables to sync. For example, 'users,orders'. For all tables, use '*'
</ParamField>
</Card>





{/* AUTO-GENERATED CONTENT END */}
