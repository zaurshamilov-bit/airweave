name: api
environments:
  Production:
    docs: The production environment that points to the production API.
    url: https://api.airweave.ai
  Local:
    docs: The local environment that points to your local API.
    url: http://localhost:8001


default-environment: Production
